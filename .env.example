# Ruta al archivo SQLite (opcional; por defecto ./data/contacts.db)
DATABASE_PATH=./data/contacts.db

# Puerto del servidor (opcional; por defecto 3000)
PORT=3000

# Entorno: development | production
NODE_ENV=development
